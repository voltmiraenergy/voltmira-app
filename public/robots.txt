User-agent: *
Allow: /
Disallow: /api/
# /demo provisions a real (disposable) workspace per visit, so a crawler hitting
# it would create tenants. The landing page links to it for humans; there is
# nothing here worth indexing anyway, since it just redirects to the app.
Disallow: /demo

Sitemap: https://voltmira.com/sitemap.xml
